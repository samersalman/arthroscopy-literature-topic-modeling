# Environment Variables — BERTopic Sports Medicine Study
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# Required: NCBI Entrez API (PubMed)
PUBMED_EMAIL=your_email@institution.edu
PUBMED_API_KEY=                          # Optional but increases rate limit (10 req/s vs 3 req/s)
                                         # Get free key at: https://www.ncbi.nlm.nih.gov/account/

# Optional: OpenAI (only needed for LLM topic labeling in Phase 6)
OPENAI_API_KEY=sk-...
